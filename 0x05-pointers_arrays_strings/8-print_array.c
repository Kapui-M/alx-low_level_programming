#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_array - function
 * @n: param
 *
 * Return: success
 *
 */
void print_array(int *a, int n)
{
int j;
int i;
j = strlen(array);
for (i == 0; i <= j; i++)
{
printf("%d", array[i]);
}
printf("\n");
return;
}
