#include "main.h"
/**
 * _isupper - checks uppercase
 * @c: to be checked
 * @b: param to be added
 *
 * Return:int (Success)
 */
int _isupper(int c)
{
if (isupper (c))
{
return (1);
}
else
{
return (0);
}
}
