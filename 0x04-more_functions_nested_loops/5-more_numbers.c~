#include "main.h"
/**
 * more_numbers - multiplies two integers
 * @a: value one
 * @b: value two
 *
 * Return:int (Success)
 */
void more_numbers(void)
{
int i;
int j;
for (j = 0; j <= 10; j++)
{
for (i = 0; i <= 14; i++)
{
putchar('0'+i);
}
putchar('\n');
}
}
